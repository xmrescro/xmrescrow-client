static/monero.js -text
static/monero.worker.js -text
# Everything under static/ is served byte-exact and its SHA-256 is published in
# HASHES.txt. Git must never rewrite line endings here, or a Windows checkout
# hashes differently from the served file and honest verification fails.
static/** -text
